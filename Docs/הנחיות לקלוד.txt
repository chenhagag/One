I want to build step 1 of a completely new conversational frontend and chat flow.

CRITICAL:
Do NOT delete, replace, or modify the existing old frontend/user dashboard/chat flow.
Do NOT modify the existing analysis model/analysis agent.
The current analysis flow must remain exactly as it is.
This new chat is completely separate from:
1. the old frontend
2. the old chat flow
3. the analysis agent

Eventually, after the new frontend stabilizes, the old frontend may be deleted — but NOT now.

Reference:
There is an image in the Docs folder named frontScatch.
Use it as the visual reference for the new frontend.
The UI should be clean, professional, ChatGPT-like, RTL Hebrew, and visually close to the sketch.

Scope for this step:
1. Create a new frontend page/screen for the new conversational experience.

2. The page should include:
   - left sidebar with logo/name
   - sidebar items:
     - חזרה לשיחה
     - הפרטים שלי
     - פרופיל
     - תובנות על עצמי
     - בדיקת טעם אישי
     - הגדרות
   - bottom user area
   - main chat area
   - header with “שיחה חדשה”
   - welcome block similar to the sketch
   - message input area
   - suggestion buttons shown visually only

3. For now:
   - sidebar buttons do not need to work
   - suggestion buttons do not need to work
   - they only need to appear visually

4. The only real functionality required now:
   - user can type a message
   - user can send the message
   - the message appears in the chat
   - the system responds through a new simple conversational agent
   - this must be a new backend/frontend flow, not the old legacy chat

5. Add access from the admin user screen:
   - Today there is a button like “צפייה במסך משתמש” that leads to the existing user dashboard.
   - Add another button that opens the new conversational frontend for that user.
   - Suggested label: “צפייה בממשק השיחה החדש”
   - This should route to the new frontend page with the selected user id.

6. Keep this implementation minimal.
Do not add:
   - RAG
   - memory
   - orchestrator
   - profile analysis
   - readiness logic
   - voice
   - advanced modes
   - matching logic

These will be added later.

7. New conversational agent behavior:
The agent should NOT introduce itself as MatchMe.
It should act as a professional conversational expert in psychological/personality understanding for matchmaking.
The conversation must feel fully natural and flowing, not like a questionnaire.

Suggested system prompt:

“You are a professional conversational expert specializing in psychological and personality understanding for matchmaking.
Your role is to hold a natural, warm, intelligent Hebrew conversation with the user in order to gradually understand who they are, what kind of relationship may suit them, and what kind of partner would fit them.

Speak in fluent, natural Hebrew.
Be warm, professional, calm, and emotionally intelligent.
Do not sound like a form, questionnaire, therapist cliché, or sales bot.
Ask one good follow-up question at a time.
Keep the conversation flowing naturally.
Adapt slightly to the user’s tone and level of openness.
Do not produce structured analysis.
Do not score traits.
Do not mention internal models, analysis agents, algorithms, or system architecture.
Do not claim that a match is ready.
At this stage, only conduct simple natural conversation.”

8. Before making changes:
Please inspect the current frontend/backend routing structure.
Then briefly summarize which files you plan to create or edit.

9. After implementation, provide a short summary of:
   - files changed
   - new route/url
   - how to test it
   - anything intentionally left as placeholder

Expected result:
- Existing old frontend/user dashboard/chat still works unchanged.
- Existing analysis agent remains unchanged.
- Admin user screen has a new button to open the new chat frontend.
- New frontend visually resembles Docs/frontScatch.
- New chat can send/receive simple messages.
- No existing analysis or matching logic is affected.