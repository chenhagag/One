## Conversation Transcript

{{transcript}}

## Trait Definitions

### Internal Traits (personality/behavioral)
{{internal_trait_definitions}}

### External Traits (appearance/physical)
{{external_trait_definitions}}

## Existing Profile (if any)
{{existing_profile}}

## Your Task

Analyze the transcript and extract trait assessments. Maximize coverage — a weak assessment beats a missing trait.

### Step 1: Internal traits

For EVERY internal trait definition, check for any signal (direct, indirect, or contextual):
- Lifestyle → energy_level, party_orientation, hippie_style, tel_aviv_style, mainstream_style
- Personality cues → neuroticism, emotional_stability, warmth, seriousness, openness
- Writing style → cognitive_profile
- Cultural signals → style traits, value_rigidity, religiosity
- Partner preferences → weight_for_match on relevant traits

Output per trait: trait_id, internal_name, score (0-100), confidence (0.0-1.0), weight_for_match (null or 0-10), weight_confidence (null or 0.0-1.0).

### Step 2: External traits

For each external trait, assess personal_value and desired_value INDEPENDENTLY:
- personal_value: only from self-description ("I am...", "I have...", "I look...")
- desired_value: from any partner preference or attraction signal

Use trait_id and internal_name from the definitions. Use the closest possible_value when mapping natural language (e.g. "sturdy" → "muscular" for body_type).

### Step 3: Missing traits and completeness

- missing_traits: only traits with truly zero signal
- recommended_probes: 3-5 topics to fill the biggest gaps
- profiling_completeness: { internal_assessed, internal_total ({{internal_total}}), external_assessed, external_total ({{external_total}}), coverage_pct, ready_for_matching (true if >60%), notes }

Return ONLY valid JSON. No explanation, no markdown fences.
