## Conversation Transcript

{{transcript}}

## Trait Definitions

### Internal Traits (personality/behavioral)
{{internal_trait_definitions}}

### External Traits (appearance/physical)
{{external_trait_definitions}}

## Existing Profile (if any)
{{existing_profile}}

## Instructions

Analyze the conversation transcript above and extract as many trait assessments as possible.

IMPORTANT: Be thorough. Even weak or indirect signals should be captured with low confidence.
- "I go out a lot" → party_orientation: high score, moderate confidence
- "I'm at the gym 3 times a week" → energy_level: high score, good confidence
- She seems articulate → cognitive_profile: moderate-high score, low confidence
- She's secular → religiosity: low score, good confidence
- No mention of politics → omit (no signal at all)

Go through EVERY trait in the list and ask: "Is there ANY signal in the conversation for this?"
If yes (even weak), include it. If truly zero signal, omit it and list in missing_traits.

For each internal trait you assess, output:
- trait_id: the ID from the trait definitions
- internal_name: the internal_name from the trait definitions
- score: 0-100
- confidence: 0.0-1.0
- weight_for_match: null OR 0-10. Use null when there is no signal about partner preference (this is the default for most traits). Use a number ONLY when the user expressed clear preference. Use 0 ONLY if the user explicitly said this trait doesn't matter in a partner.
- weight_confidence: null if weight_for_match is null. Otherwise 0.0-1.0.

For each external trait you assess, output:
- trait_id: the ID from the trait definitions
- internal_name: the internal_name from the trait definitions
- personal_value: the user's own value (use values from possible_values when available)
- personal_value_confidence: 0.0-1.0
- desired_value: what the user wants in a partner (first-class field — infer when possible)
- desired_value_confidence: 0.0-1.0
- weight_for_match: null by default. Use a number only if the user expressed how important this trait is. Use 0 only if they explicitly said it doesn't matter.
- weight_confidence: null if weight_for_match is null. Otherwise 0.0-1.0.

Also provide:
- missing_traits: internal_names of traits with truly zero signal
- recommended_probes: 3-5 conversation topics to fill the biggest gaps
- profiling_completeness: { internal_assessed, internal_total (use {{internal_total}}), external_assessed, external_total (use {{external_total}}), coverage_pct, ready_for_matching (true if coverage > 60%), notes }

Return ONLY valid JSON. No explanation, no markdown fences.
